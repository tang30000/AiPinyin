# AiPinyin 配置文件
# 放置于 aipinyin.exe 同目录

[engine]
# 候选词模式: "ai" = AI主导(字典兜底), "dict" = 字典主导(AI重排)
mode = "ai"

[ai]
# AI 候选占位数 (AI主导模式下, 前N位给AI, 剩余给字典)
top_k = 3
# AI 是否参与字典候选排序 (true/false)
rerank = false

[ui]
# 候选窗口字体大小
font_size = 16
# 候选窗口透明度 (0-255, 255=不透明)
opacity = 240
